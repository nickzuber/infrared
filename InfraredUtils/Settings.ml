(* We print the intermediate steps and data structures when in debug mode. *)
let debug_mode = ref false

(* We print the inferred types in the console for each identifier in each file. *)
let show_types_in_console = ref false
